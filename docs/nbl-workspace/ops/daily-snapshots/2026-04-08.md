# Daily Snapshot — 2026-04-08

## 今日の主な成果

### 次期公開版リリース（完了）

- **ホームページ刷新**: home-a（ダークヒーロー）デザインを `/` に昇格
  - デザインA/B比較レビューを経て、Aを選択
  - Aboutセクションにビジョン（AI時代の人間参加）を追加
- **`/about` ページ更新**: 「NBLの事業の地平」セクションを追加
  - `NBLの仕事設計.jpg` インフォグラフィックを掲載
  - テキストを「就労支援起点→AI時代の参加設計」に統一
- **`/resources/disability-work-design`** 新規公開
  - 10障害分類 × 仕事設計の視点インフォグラフィックシリーズ
- **`/preview/home-a`、`/preview/home-b`** プレビューページ（比較用、非インデックス）

### インフラ修正

- `next.config.js`: `/resources` → `/knowledge` の永続リダイレクト削除（`/resources` が実ページになったため）
- `next.config.js`: `localPatterns` に `/resources/disability-work-design/**`、`/about/**` 追加

### はたらく相談室（本セッション累積）

- FCHMAアトラス全体スキャン＋複数仮説並列提示アーキテクチャ
- ドメイン/モチーフラベルを仮説カードに表示
- 信頼度ベース終了ロジック（fixed 2回 → max confidence medium以上で打ち切り）
- 4層知識制約をシステムプロンプトに追加
- 合意文書に相談内容・見立て要旨・AI総合解説を追加
- `/api/jac-synthesis` 新規エンドポイント
- IPS/SE説明を平易な日本語に改善

---

## 未解決・次期開発への引き継ぎ

### 優先度 高

1. **利用制限のVercel環境変数確認**
   - `.env.local` のテスト用に引き上げた値をVercelダッシュボードの本番値と比較・整合させる
   - 推奨本番値: `JAC_RATE_LIMIT_PER_DAY=30`、`JAC_GLOBAL_RATE_LIMIT_PER_DAY=500`、`JAC_COSTLY_RATE_LIMIT_PER_TOKEN_PER_DAY=10`
   - **レート制限の仕組み**: IPベース（トークンなし）。デバイス・ネットワーク変更=IP変更=カウントリセット。トークンありの場合はトークン単位で追跡（デバイス変更でもリセットされない）。

2. **画像の軽量化（本番デプロイ前推奨）**
   - 現状: 56枚 × 平均5〜7MB = 257MB
   - Next.js Image が AVIF/WebP に自動変換するため表示は最適化済みだが、Vercel帯域・ビルド時間に影響
   - sharp が `package.json` に含まれているので圧縮スクリプト作成可能
   - 対象: `/public/resources/**/*.png`、`/public/jac-foundations/**/*.png`
   - 目標: 各ファイル 600KB〜1MB 程度

3. **creator gate（JAC_PUBLIC_ENABLED）**
   - Vercelの環境変数で `JAC_PUBLIC_ENABLED=true` になっていることを確認してデプロイ

### 優先度 中

4. **`/resources` ページ SiteNav整合**
   - SiteNavに「Resources」追加済み。`/resources` は実ページ。
   - `next.config.js` のリダイレクト削除済み。動作確認を本番で行う。

5. **`/resources/disability-work-design` の画像ファイル**
   - 現在、画像は `/public/resources/disability-work-design/` に配置済み
   - 実際の画像ファイルがContent-inboxのPNGから正しくコピーされているか確認

6. **`/about` インフォグラフィック表示確認**
   - `/public/about/nbl-work-design-vision.jpg` が正しく配置済み
   - 本番でのレイアウト確認

### 優先度 低（次バージョン）

7. **`/organizations/diagnosis`** — Layer C Q14地域差データ可視化
8. **地域差可視化コンポーネント** — 北海道33% vs 香川5.3%
9. **ナビゲーション整理** — 現行の冗長なリンク・リダイレクト整理
10. **`/preview/home-a`、`/preview/home-b` の削除** — 本番公開後に不要になる

---

## セキュリティチェック結果

- **APIルート**: 全エンドポイントが `guardJacApiRequest` で保護済み ✓
- **入力バリデーション**: `/api/jac-synthesis` は `consultation` 必須チェックあり ✓
- **プロンプトインジェクション**: 相談文がOpenAIプロンプトに直接埋め込まれるが、出力はテキスト合成のみ（実行可能コードなし）。リスク低 ✓
- **レート制限**: per-IP + global + per-token の3層制御 ✓
- **XSS**: Reactのエスケープ処理により自動的に対応 ✓
- **セキュリティヘッダー**: `Referrer-Policy`、`X-Content-Type-Options` 設定済み ✓
- **潜在的改善点**: `/api/jac-synthesis` のOpenAIエラーレスポンスがクライアントに返る（`errText` 露出）。重大ではないが将来的に汎用エラーメッセージへの置き換え推奨。

---

## 次期バージョンのテーマ案

1. **はたらく相談室 精度向上ループ**: フィードバックデータから実際の仮説精度を評価
2. **Layer B/C コンテンツ展開**: 実践知識・組織診断の充実
3. **障害別仕事設計インフォグラフィック拡充**: 地域別データとの接続
4. **画像軽量化パイプライン**: シャープによる自動圧縮をビルドスクリプトに組み込み
