# Content Workflow

NBL の公開候補コンテンツは、まず `content-inbox` から始める。

## 流れ

1. まず `content-inbox` に素材を入れる
2. review したいものを `content-review` に移す
3. 公開可なら `content-approved` に移す
4. 今は出さないものは `content-hold` に移す

## Founder の使い方

- 新しい素材を入れるときは `content-inbox/` に置く
- 必要なら `content-inbox/founder-new-content-log.md` に短い説明を足す
- サイトを使って気づいたことは `content-inbox/founder-site-feedback-log.md` に足す
- 長文は不要。`何か / 誰向けか / 何に使いたいか`、または `page / issue / desired effect` だけでよい

こちらで triage して、`Public Narrative / Knowledge & Method / Business Validation / Next Horizon` のどこに流すか整理する。

## 迷ったとき

- 未完成なら `content-inbox`
- 判断待ちなら `content-review`
- 実装に使ってよいなら `content-approved`
- 今は出さないなら `content-hold`
